# Supabase - get these from your Supabase project settings
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# For production (e.g. Netlify) - used by auth callback for redirect origin
# Set to your deployed URL, e.g. https://cognitiveos-app.netlify.app
# Also add this URL (and https://YOUR-SITE.netlify.app/auth/callback) to Supabase:
# Dashboard → Authentication → URL Configuration → Redirect URLs
NEXT_PUBLIC_SITE_URL=
